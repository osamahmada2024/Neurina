services:
  mongodb:
    image: mongo:latest

    container_name: mongodb

    ports:
      - "27017:27017"
    
    volumes:
      - ./mongodb_data:/data/db

    networks:
      - backend

    restart: always

  face-crop-service:
    build:
      context: ./face_crop_service
      dockerfile: Dockerfile
    container_name: face-crop-service
    ports:
      - "8010:8010"
    networks:
      - backend
    restart: always
  
networks:
  backend:
  